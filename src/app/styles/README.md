# `app/styles`

The wireframe's design tokens, typography, layout, and interaction states.

Typography is a document-level setting (DESIGN.md D6): font family, size, line
height, line width, and theme are preferences, never per-selection marks.
Filled by EDITOR-1.
