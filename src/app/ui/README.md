# `app/ui`

Reusable editor chrome extracted from `docs/wireframe.html`: the calm canvas,
formatting bar, Mermaid block, and saved/error states.

The wireframe is the visual specification and extraction source. Its inline
demo state and scripted state-switching are reference material, not runtime
architecture. Filled by EDITOR-1.
